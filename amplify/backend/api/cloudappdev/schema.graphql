type Note @model {
  id: ID!
  name: String!
  description: String
  image: String
}

type HRV @model{
  id: ID!
  value: String!
}

type UserStats @model{
  id: ID!
  username: String!
  height: String!
  weight: String!
  age: String!
}

type RHR @model{
  id: ID!
  value: String!
}

type Temperature @model{
  id: ID!
  value: String!
}
